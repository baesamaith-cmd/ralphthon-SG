# Architecture

This file must be rewritten for the actual Ralphthon project before implementation begins.

## Current Shape

- This repository is a scaffold for planning and running a Ralph loop.
- Product code has not been chosen yet.
- Tickets will be generated after the interview phase.

## Stack

TBD after the interview.

Record:

- language and framework
- package manager
- app entry points
- test locations
- build commands
- lint/typecheck commands
- required environment variables

## Verification

TBD after the project stack is chosen.

Every ticket must include a concrete verification command. Examples:

```bash
npm test
```

```bash
python3 -m unittest
```

```bash
npm run lint && npm run build
```

## Design Preferences

- Keep implementation scoped to the current ticket.
- Prefer boring, readable code over clever abstractions.
- Preserve existing behavior unless the ticket says otherwise.
- Add tests or verification artifacts for every new behavior.
- Do not refactor unrelated code.
- Make the harness, specs, and verification visible enough to demo.

